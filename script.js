const tasks = [];
const today = false;

function showTasks() {
  /* if today, for loop to show all tasks in today section
  else show in tomorrow section */
}

function newTask() {
  /* add to end of array (push, i think) */
}

function newDay() {
  /* move all tasks to Today status, run showTasks */
  /* also need to remove all tasks with .done from the array */
}

function checkOff() {
  /* button to check off task - set .done class */
}

function delay () {
  /* button for this - set to .tomorrow class, regenerate lists */
}
